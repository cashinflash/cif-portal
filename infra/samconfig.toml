version = 0.1

[default.global.parameters]
stack_name = "cif-portal-loans-dev"

[default.deploy.parameters]
region = "us-east-1"
capabilities = "CAPABILITY_IAM"
resolve_s3 = true
confirm_changeset = false
fail_on_empty_changeset = false
# Fill these in once the base stack outputs are retrieved from
# `aws cloudformation describe-stacks --stack-name cif-portal-dev`.
# parameter_overrides = "HttpApiId=... JwtAuthorizerId=... VergentSecretArn=arn:aws:secretsmanager:us-east-1:730667140069:secret:cif-portal/vergent/credentials-XXXXXX"
